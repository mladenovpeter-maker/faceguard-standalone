{
  "name": "faceguard-standalone",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server --filter @workspace/face-guard run typecheck",
    "build": "pnpm run typecheck && pnpm --filter @workspace/api-server --filter @workspace/face-guard run build"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
