FROM node:20-alpine

RUN apk add --no-cache docker-cli docker-cli-compose git bash

WORKDIR /app
COPY selfhost/mcp-server/package.json .
RUN npm install --omit=dev

COPY selfhost/mcp-server/server.mjs .

EXPOSE 9100
CMD ["node", "server.mjs"]
